{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        ".auto/build/**",
        ".next/**"
      ]
    },
    "lint": {
      "outputs": []
    }
  }
}
